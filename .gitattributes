# Листы, спеки и дампы сравниваются построчно — перевод строки в репозитории всегда LF.
# Иначе Windows-чекаут переписал бы все 72 index.html в CRLF и первый же diff стал бы бесполезным.
* text=auto eol=lf

# Шрифт вшит в CSS как base64 — это текст, но одной строкой; отмечать нечего.
*.woff2 binary
*.png   binary
