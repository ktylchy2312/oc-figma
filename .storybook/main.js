/**
 * Storybook — витрина над code/, а не отдельное представление компонентов.
 *
 * Истории лежат рядом с кодом, который показывают, и генерируются тем же build-code.mjs.
 * Отдельная папка со сторями означала бы второй источник разметки, который начнёт расходиться
 * с первым в тот день, когда кто-то поправит только один из них.
 */
export default {
  // Пакет собирается и проверяется с закрытой сетью — телеметрия здесь была бы единственным
  // исходящим запросом во всей цепочке.
  core: { disableTelemetry: true },
  stories: ["../code/*/*.stories.js"],
  addons: ["@storybook/addon-docs"],
  framework: { name: "@storybook/html-vite", options: {} },
};
